{
  "name": "tower-defence",
  "version": "0.1.0",
  "private": true,
  "description": "A small browser tower defence game, built by the Loop-Forge Dev-Loop",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"src/**/*.test.js\" \"scripts/**/*.test.mjs\"",
    "start": "node scripts/serve.mjs"
  }
}
